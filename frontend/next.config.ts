import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['127.0.0.1', 'img.daisyui.com',"queue-management-y00x.onrender.com", 'my-queue-bucket-isp.s3.amazonaws.com'],
  },
};

export default nextConfig;